import React from 'react';
import { InnerPage, PageHeader } from '@kubevious/ui-components';

export const AboutPage = () => {

    return (
        <InnerPage narrow header={<PageHeader title="About" />}>

        </InnerPage>
    );
};
